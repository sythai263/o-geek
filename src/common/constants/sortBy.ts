export enum SortBy {
    ID = 'id',
    USER_ID = 'user.id',
    USER_ALIAS = 'user.alias',
    USER_NAME = 'user.name',
    START_DATE = 'startDate',
    COMMIT_ID = 'committedWorkload.id',
    PLANED_ID = 'plannedWorkload.id',
    CREATED_AT = 'createdAt',
    UPDATED_AT = 'updatedAt',
    STATUS = 'status',
}
